
/** Renders an error message
 * 
 * State:
 *  - none
 * 
 * Props:
 *  - message string
 * 
 */
function Error({ message }) {
    return (
        <div>
            {message}
        </div>
    );
}

export default Error;